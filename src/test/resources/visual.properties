visual.profile=desktop
visual.browser=chromium
visual.headless=true

# Keep value -1 to run with parameters
visual.viewport.width=-1
visual.viewport.height=-1

visual.threshold.percent=0.15

visual.baseline.dir=artifacts/visual/baseline
visual.actual.dir=artifacts/visual/actual
visual.diff.dir=artifacts/visual/diff

visual.updateBaseline=false